# Backend (Express + MongoDB Atlas)

## Quick Start

1. Install dependencies:
   - `npm install`
2. Create `.env` based on `.env.example`
3. Run development server:
   - `npm run dev`

## Notes

- Auth uses HTTP-only cookie (`token`)
- CORS allows frontend URL from `FRONTEND_URL`
- All route handlers and modules include inline comments for easier understanding
